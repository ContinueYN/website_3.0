services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: contact_backend
    ports:
      - "3001:3001"
    working_dir: /app/backend
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
